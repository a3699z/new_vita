export interface Feature {
    dataPrivacy?: DataPrivacy | boolean;
    fileSharing?: boolean;
    networkTest?: boolean;
    chat?: ChatType | null;
}
export interface DataPrivacy {
    termsUrl: string;
    dataPrivacyUrl: string;
    applicationName?: string;
}
export declare type ChatType = 'embedded' | 'custom';
