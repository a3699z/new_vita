export interface SessionInfo {
    apiKey: string;
    sessionId: string;
    token: string;
}
export interface SessionInfoResponse extends SessionInfo {
    preJoin: SessionInfo;
}
export declare enum ConnectionStatus {
    CONNECTING = "connecting",
    CONNECTED = "connected",
    DISCONNECTED = "disconnected"
}
export declare enum DisconnectionReason {
    CLIENT_DISCONNECTED = "clientDisconnected"
}
export interface CustomServer {
    urls: string | string[];
    username?: string;
    credential?: string;
}
export declare type TurnConfig = CustomServer[];
export interface IceConfig {
    includeServers: 'all' | 'custom';
    transportPolicy: 'all' | 'relay';
    customServers: CustomServer[];
}
export declare enum StreamEvent {
    STREAM_PROPERTY_CHANGED = "streamPropertyChanged"
}
